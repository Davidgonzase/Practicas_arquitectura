import { assertEquals } from "https://deno.land/std@0.203.0/assert/mod.ts";
import { fetchquestions } from "./main.ts";

//La unica funcion que verdaderamente debo de probar es la que me devuelve el array de preguntas del api pero al ser una funcion asincrona el test me da error

Deno.test(function addTest() {
});
